export const AppCore = {
  HOST: 'http://localhost/b2b-back',
};

// Table表格的 columns 初始化
// export function TableColumnsCfg(mod) {
//   let columns = [];
//   Object.keys(mod.list).forEach((item, index, list) => {
//     let itemData = {};
//     itemData.title = item.text;
//     // 如果存在 className
//     if (item.cellClass) {
//       itemData.className = item.cellClass;
//     }
//     // 如果存在img
//     if (item.img) {
//     }
//     // 如果存在type
//     if (item.type) {
//     }
//     // 如果存在trigger
//     if (item.trigger) {
//     }

// 		// 如果存在级联cascade
// 		if (item.cascade) {
// 		}
// 		if (item.cascade2) {
// 		}
// 		// 如果存在tip
// 		if (item.tip) {
// 		}
// 		// 如果存在ro
// 		if (item.ro) {
// 		}
//   });
//   return columns;
// }
